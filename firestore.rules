rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /calendars/{calendar} {
      allow read, write;
    }
    match /calendars/{calendar}/users/{user} {
      allow read, write;
    }
  }
}
